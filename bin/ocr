#!/bin/ruby

require 'ocr'

if ARGV.first == '--no-validation'
  ARGV.shift
  reader = OCR::AccountReader.new($stdin, validate: false)
else
  reader = OCR::AccountReader.new($stdin)
end

reader.each do |account_number|
  puts account_number.show
end
